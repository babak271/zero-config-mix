# Zero configured laravel&-mix

Use this repository to develop your frontend with HMR and browser live reload pre-configured.

## Usage

after cloning, run below commands

1. `composer install`
2. `npm install`
3. `npm run hot`
